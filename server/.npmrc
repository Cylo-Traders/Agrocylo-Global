# Fail `npm install` / `npm ci` immediately on an unsupported Node runtime instead
# of letting Prisma's engine check fail later with a less specific error.
# The supported range lives in package.json → engines.node.
engine-strict=true
